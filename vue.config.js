// github
module.exports = {
  // publicPath: process.env.NODE_ENV === 'production' ? '/vue-industry-gis/' : '/'
  publicPath: ''//用来进行本地访问，线上服务器需要另外设置。
}

// Electron
// module.exports = {
//   publicPath: process.env.NODE_ENV === 'production' ? './' : './'
// }

// 腾讯云
// module.exports = {
//   publicPath: process.env.NODE_ENV === 'production' ? '/' : '/'
// }